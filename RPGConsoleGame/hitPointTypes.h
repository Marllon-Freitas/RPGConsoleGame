#pragma once

#include <cstdint>

// This is the type of the hp points are chosen, it is a 16-bit unsigned integer. 
// If we need to change the type of the hp points, we can change it here.
typedef uint16_t hp_t;
